---
name: Example Member
voicePart: Tenor
# leadershipRole: Music Director
draft: true
# Optional: photo must start with /images/team/profiles/
# photo: /images/team/profiles/example.jpg
---

This entry has `draft: true` so it is hidden on the public meet-the-team page. Duplicate the file, set `draft: false`, and add an optional `photo` path starting with `/images/team/profiles/`. Profile images: export **square (1:1)** at **800×800** (Canva Image Resize), max **1 MB** — see `src/constants/profile-media.json` and README headshot steps.
